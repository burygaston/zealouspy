"""Failing test demonstration for testing quarantine policies."""

import pytest


def test_always_fails():
    """This test always fails to demonstrate quarantine behavior."""
    assert True, "Test fixed - no longer failing"
